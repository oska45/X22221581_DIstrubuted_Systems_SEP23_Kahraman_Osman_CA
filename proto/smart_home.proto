syntax = "proto3";

service SmartHome {
  rpc UpdateSmartHomeState (stream UpdateRequest) returns (stream UpdateResponse) {}
}

message UpdateRequest {
  string message = 1; // Type of action (e.g., turn on lights, increase temperature)
}

message UpdateResponse {
  string message = 1; // Response message from the server
}
